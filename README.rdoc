== README

To get this application up and running, you'll need to first install Ruby version 2.3.3.

After cloning the repo, you'll need to run these commands.

$ bundle install
$ rake db:migrate
$ rails server

In your browser, go to: localhost:3000


<tt>rake doc:app</tt>.
